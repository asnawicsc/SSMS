<h2>New Teacher attendance</h2>

<%= render "form.html", Map.put(assigns, :action, teacher_attendance_path(@conn, :create)) %>

<span><%= link "Back", to: teacher_attendance_path(@conn, :index) %></span>
