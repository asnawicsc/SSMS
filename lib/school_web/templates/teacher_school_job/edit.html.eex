<h2>Edit Teacher school job</h2>

<%= render "form.html", Map.put(assigns, :action, teacher_school_job_path(@conn, :update, @teacher_school_job)) %>

<span><%= link "Back", to: teacher_path(@conn, :teacher_setting) %></span>
