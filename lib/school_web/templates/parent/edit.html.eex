
<div class="content-heading">
    <div><%= gettext "Edit Parent" %>
    </div>
</div>

<%= render "form.html", Map.put(assigns, :action, parent_path(@conn, :update, @parent)) %>

<span><%= link "Back", to: parent_path(@conn, :index) %></span>
